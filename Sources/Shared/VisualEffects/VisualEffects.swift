/// Portable visual-effects kit — brand-free (no Even tokens).
/// Destined for a shared components package; keep it that way.
///
/// Loading skeletons: ``View/loading(_:animation:)`` — redacted + shimmer,
/// toggled with an animated boolean.
public enum VisualEffectsModule {
    public static let name = "VisualEffects"
}
